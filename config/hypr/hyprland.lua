-- Loaded automatically by HyDE's hyde.lua via check_require("hyprland").
-- Do NOT require("hyde") or dofile hyde.lua here -- hyde.lua is the actual
-- entry point (HYPRLAND_CONFIG points at it); this file is pulled in from
-- inside its own load chain. Requiring hyde.lua from here would recurse.

require("monitors")
require("nvidia")
require("userprefs")
require("keybindings")
require("windowrules")
